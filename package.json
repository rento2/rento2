{
  "name": "root",
  "private": true,
  "devDependencies": {
    "husky": "^7.0.0",
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "prepare": "husky install",
    "precommit:admin": "cd packages/admin && npm run precommit",
    "precommit:backend": "cd packages/backend && npm run precommit",
    "precommit": "npm-run-all precommit:*"
  }
}
