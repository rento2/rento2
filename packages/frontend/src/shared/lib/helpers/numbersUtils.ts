const roundInteger = (number: number): number => {
  return Math.round(number)
}

const convertNumber = (number: string): number => {
  return Number(number)
}

export { roundInteger, convertNumber }
